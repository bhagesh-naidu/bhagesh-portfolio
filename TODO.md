# TODO

- [ ] Replace first page hero image (`heroBold`) with `Gemini_Generated_Image_yu5cueyu5cueyu5c.png`.
- [ ] Ensure background is removed and image renders cleanly (use the PNG’s transparency). If needed, adjust layering/gradients so transparency shows correctly.
- [ ] Keep it perfectly aligned with existing hero object-fit/positioning and current scroll/scale motion.
- [ ] Run build/dev checks (if available) to confirm no TS/asset import errors.

